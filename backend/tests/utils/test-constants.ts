export const TEST_CATEGORIES = {
  /** Tests functions that classify values by their properties or characteristics. */
  CLASSIFICATION: "Classification",

  /** Tests functions that convert values between representations or formats. */
  CONVERSION: "Conversion",

  /** Tests functions that verify whether values or operations satisfy constraints. */
  VALIDATION: "Validation",

  /** Tests functions that modify, transform, or manipulate values. */
  MANIPULATION: "Manipulation",

  /** Tests bidirectional or correspondence-based mappings between values. */
  MAPPING: "Mapping",

  /** Tests functions that count or measure the population of set elements. */
  POPULATION: "Population",

  /** Tests functions that determine or perform movement-related behavior. */
  MOVEMENT: "Movement",

  /** Tests functions that deterministically derive or calculate values. */
  COMPUTATION: "Computation",

  /** Tests functions that retrieve or derive specific information from values. */
  EXTRACTION: "Extraction",
  INSERTION: "Insertion",

  /** Tests functions that determine relationships between multiple values. */
  RELATIONSHIP: "Relationship",

  /** Tests functions that calculate distances or separation between values. */
  DISTANCE: "Distance",

  /** Tests functions that create or produce new values, objects, or structures. */
  GENERATION: "Generation",

  /** Tests functions that parse structured or external representations into values. */
  PARSING: "Parsing",

  COMPOSITION: "Composition",

  /** Tests functions that serialize values into structured or external representations. */
  SERIALIZATION: "Serialization",
  FORMATTING: "Formatting",

  STATE: "State",
  BITBOARDS: "Bitboards",
  PIECES: "Pieces",
  BOARD: "Board",
  PROPERTIES: "Properties",
  INTEGRATION: "Integration",
  CREATION: "Creation",

  TURN: "Turn",
};
