import { describe } from "vitest";
import * as TestMagicGenerate from "@/domain/chess/movegen/magic/magic.generate.js";

describe("magic.generate", () => {
  describe(TestMagicGenerate.generateMagics.name, () => {});
});
